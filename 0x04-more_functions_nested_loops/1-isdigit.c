#include "main.h"

/**
 * _isdigit - Print alpha
 * @c: digit
 *
 * Return: 1 if digit,0 if otherwise
 */
int _isdigit(int c)
{
	int d = 0;

	if (c >= d && c <= 9)
	{
		return (1);
	}
	else
		return (0);
}
